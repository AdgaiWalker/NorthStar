# 执行清单
- [ ] 确认本机 pnpm 版本，确定 `packageManager` 精确值。
- [ ] 探测项目是否已有 lint 配置（如 `.eslintrc*`、`eslint.config.*`），决定 `scripts.lint` 的实际命令或占位。
- [ ] 在根目录生成 `package.json`（≤50 行），填入约定字段与安全默认值。
- [ ] 运行 `pnpm lint` 验证脚本可用并记录结果。
