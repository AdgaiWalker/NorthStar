# 规格增量：同步与待创建事项

## 1. 应同步到主规格（PRD-盘根AI指南针-标准版.md）
- AI 登录/额度规则：改为“游客可用，额度 3+3/天，本地存储，0 点重置；未登录不作硬性拦截”并在后续迭代改回登录扣次。
- fallbackReason 枚举：新增 `quota_exhausted`。
- 阅读页：声明受控块语法 `:::video{...}:::`、`:::callout{...}:::` 及双链行为；将 isVideo 描述改为“视频以受控块呈现，可多块”。
- 普通搜索：说明当前实现匹配 name/description/tags，未分页（V1）。
- 方案生成：V1 生成结果页内提示/重试，成功后手动保存并跳转；未实现登录门槛，文档降级说明。
- 用户后台：V1 仅 profile/solutions/favorites/settings；其他导航锁定并标注后续版本。
- User/Audit/Review 契约：按当前代码字段/枚举降级为 “V1 Demo 简化”，保留后续目标字段。

## 2. 新增拆分 Spec（待创建，需在后续合入）
1) `Spec-文档系统-混排块与双链.md`  
   - 受控块语法（video/callout）、校验规则、TOC/slug 规则、双链解析与不存在行为（删除线/Locked 提示）。
2) `Spec-审核工作台-任务模型与自动分配.md`  
   - ReviewTask 状态机（含 pending_review/revision/unassigned）、WIP=10、自动分配与待分配池恢复策略、改派与权限、AuditLog 事件。
3) `Spec-后台AI助手-交互与审计.md`  
   - 按钮集合（摘要/拒绝理由/修订总结/风险扫描）、Diff 展示、应用与撤销、审计记录字段、上下文范围说明。

## 3. 同步策略
- 本次变更先在 README/PRD 内就地更新；拆分 Spec 在后续迭代补全并链接到 PRD 对应章节。
- 安全类变更（/admin 守卫）在代码与 PRD 同步，后续接入后端鉴权时再收紧规则。
