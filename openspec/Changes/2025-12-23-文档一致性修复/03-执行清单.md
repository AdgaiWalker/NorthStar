# 执行清单（Atomic Checklist）
- [x] 守卫：`/admin` 路由增加 RequireAuth（前端占位登录/角色），消除 P0。
- [x] README：修正 PRD 链接；补充脚本（build/preview/lint/format）与 lockfile 策略；说明 AI 配置与额度规则（并明确当前 model 未开放配置）。
- [x] PRD：更新 AI 登录门槛、额度重置周期（每日 00:00）、fallback 枚举；阅读页受控块/双链；用户后台与契约降级说明；并收敛「阅读页视频区域」与「方案生成保存流程」的矛盾表述。
- [x] AISearch：普通搜索匹配 tags；文案提示与本地额度保持一致（并移除 mount useEffect setState 以满足 lint）。
- [x] SolutionNewPage：失败/额度耗尽时留在本页，提示错误并可重试，成功后保存并跳转“我的方案”（并修复 TSX 非法字符与 Hook 规则）。
- [x] 规格增量：在 04-规格增量.md 记录本次变更对主规格的更新点与待创建的 3 份拆分 Spec。
- [x] Lint：将 routes.tsx 内的 RequireAuth 组件拆到独立文件，消除 react-refresh/only-export-components 警告。
- [x] Lint：修复 ArticleReadPage 的 hooks 条件调用（useMemo/useState）。
- [x] Lint：修复 AdminView / UserCenterPage 的 useEffect 内 setState 警告。
- [x] Lint：清理 unused imports/vars（AppLayout/Layout/CardComponents/useReviewStore 等）。
- [x] Lint：修复 utils/export.ts 的 no-useless-escape。
- [x] Lint：处理 no-explicit-any warnings（DocRenderer/UserCenterView/AIService/types 等）。
