# 04-规格增量 (Spec Diff)

> 本次施工包对应的规格变更已直接合并至 `Specs/PRD-盘根AI指南针-最终版.md`，主要包含以下变更点：

## 1. 范围调整
- 明确 V1 为“主路径闭环版”，剔除支付、投稿、后台等 V2/V4 需求。

## 2. 接口变更
- AI 搜索输出新增 `mode` 与 `fallbackReason` 字段，用于支撑演示模式 UI。

## 3. 安全规范
- 明确 V1 阶段前端不持有 Key，采用开发代理或 Demo 模式兜底。

## 4. 交互细节
- 增加“删除”操作的不可撤销二次确认约束。
- 增加 Header 移动端折叠菜单约束。
