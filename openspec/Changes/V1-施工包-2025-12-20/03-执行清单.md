# 03-执行清单 (Execution Plan)

> 每项任务代码改动控制在50 行以内，确保原子化提交。

## Phase 1: 基础设施与契约 (Infrastructure)
- [x] **T1.1**: 更新 `types.ts`，同步 PRD V1 定义的 `Tool`, `Article`, `UserSolution` 结构。
- [x] **T1.2**: 封装 `storage.ts`，实现带损坏检测与重置提示的 LocalStorage 读写器。
- [x] **T1.3**: 定义 `aiContract.ts`，明确 `AISearchResult` + `AISolutionResult` 接口及回退逻辑。

## Phase 2: 核心组件与页面 (Core UI)
- [x] **T2.1**: 升级 `AppHeader` 组件，支持移动端折叠菜单与 4 种语言切换 UI（使用 react-router 导航）。
- [x] **T2.2**: 优化 `ToolCard` 与 `ArticleCard`，实现卡片点击与“选择按钮”的事件分离。
- [x] **T2.3**: 实现首页 `AISearch` 组件的“演示模式”提示条 + 库模式切换（专业库/综合库）。
- [x] **T2.4**: 重构浮动 `Dock` 组件，支持清空选择与跳转生成页。

## Phase 3: 业务逻辑与 AI (Business Logic)
- [x] **T3.1**: 实现 `AIService.ts` 的 `searchToolsWithAI`，接入真实 zhipu API 并实现完整回退逻辑。
- [x] **T3.2**: 实现 `generateSolutionWithAI`，使用真实 AI 生成方案（替代 mock）。
- [x] **T3.3**: 实现“我的方案”列表页，支持按时间倒序展示与删除（带二次确认）。
- [x] **T3.4**: 实现方案详情抽屉（Drawer）及 Markdown/TXT/CSV 导出功能。

## Phase 4: 验收与优化 (Final Polish)
- [x] **T4.1**: 全局检查“删除”操作的二次确认弹窗。
- [x] **T4.2**: 验证 Header 在移动端（<768px）的响应式表现。
- [x] **T4.3**: 执行完整主路径测试（提问 -> 推荐 -> 选工具 -> 生成 -> 保存 -> 导出）。

## MVPE 补充任务 (2025-12-22)
- [x] **M1**: 重构为 react-router 真实路由（替代 ViewState 模式）。
- [x] **M2**: 创建 Zustand 全局状态管理 `useAppStore`。
- [x] **M3**: 创建 7 个页面组件（HomePage, ToolDetailPage, ArticleReadPage, SolutionNewPage, UserCenterPage, LoginPage, AdminPage）。
- [x] **M4**: AI 搜索增加库模式切换（专业库/综合库）。
