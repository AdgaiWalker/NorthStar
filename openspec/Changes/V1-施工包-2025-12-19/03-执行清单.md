# V1 施工包（盘根 · AI 指南针）执行清单

## 1. 执行原则

- 每个任务只引入一个可验证的行为变化。
- 每个任务的代码改动控制在 50 行以内。
- 每次任务完成后，必须在本清单中将对应项标记为 `[x]`。

## 2. 原子任务列表

### 2.1 本地存储持久化

- [x] T1：在 `1pangen-ai-compass/App.tsx` 增加本地存储 Key 常量与读取辅助函数，覆盖 `themeMode`、`language`、`selectedToolIds`、`userSolutions`。
  - 预期改动：新增不超过 50 行
  - 验收：代码编译通过，未改变现有 UI 行为

- [x] T2：在 `1pangen-ai-compass/App.tsx` 将 `themeMode`、`language`、`selectedToolIds`、`userSolutions` 的 `useState` 改为惰性初始化读取本地存储。
  - 预期改动：改动不超过 50 行
  - 验收：刷新页面后状态保持一致

- [x] T3：在 `1pangen-ai-compass/App.tsx` 增加持久化写入逻辑。
  - 写入规则：每个键名独立写入
  - 预期改动：新增不超过 50 行
  - 验收：任意状态变化后刷新页面可恢复

- [x] T4：在 `1pangen-ai-compass/App.tsx` 增加本地数据损坏提示一次的 UI。
  - 提示文案：本地数据异常，系统已重置到默认状态。
  - 预期改动：新增不超过 50 行
  - 验收：当本地存储 JSON 损坏时，系统清理损坏键名并展示提示一次

### 2.2 AI 搜索与回退方案

- [ ] T5：在 `1pangen-ai-compass/App.tsx` 调整 `AISearch` 入参，传入当前领域过滤后的工具列表与内容列表。
  - 预期改动：改动不超过 20 行
  - 验收：切换领域后 AI 搜索上下文随领域变化

- [ ] T6：在 `1pangen-ai-compass/services/geminiService.ts` 实现确定性回退方案。
  - 工具规则：命中工具名置顶，使用评分从高到低补足，上限 3
  - 内容规则：优先精选内容补足，上限 2
  - 文案规则：`summary` 必须包含“演示模式”
  - 预期改动：改动不超过 50 行
  - 验收：无密钥与异常场景下仍返回非空推荐

- [ ] T7：在 `1pangen-ai-compass/components/AISearch.tsx` 增加“演示模式”提示条。
  - 触发条件：`result.summary` 包含“演示模式”
  - 预期改动：新增不超过 50 行
  - 验收：演示模式提示条可见且不影响正常结果展示

### 2.3 规格同步归档

- [ ] T8：将本次变更涉及的新增事实合并进 `openspec/Specs/PRD-盘根AI指南针-最终版.md`。
  - 合并范围：本地存储键名、损坏提示文案、回退方案的数量约束
  - 预期改动：改动不超过 50 行
  - 验收：PRD 继续满足禁用词约束
