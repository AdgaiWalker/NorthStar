# 执行清单（Atomic Checklist）：内容管理 + Obsidian 编辑器 + 草稿→发布 + 受众分层

规则：
- 每项任务为原子化改动，单项代码改动 ≤50 行。
- 标记 `【可并行】` 的任务可同时进行。
- 完成后用 `[x]` 勾选。

## A. 数据层：Content 模型 + 存储键 + guard
- [x] 【可并行】在 `pangen-ai-compass/src/types.ts` 增加 ContentType/ContentStatus/ContentVisibility/ContentItem/ContentAsset（≤50 行）
- [x] 【可并行】在 `pangen-ai-compass/src/utils/storage.ts` 增加 STORAGE_KEYS：contentItems/contentAssets（≤50 行）
- [x] 【可并行】在 `pangen-ai-compass/src/utils/guards.ts` 增加 isContentItemArray/isContentAssetArray（≤50 行）

## B. Store：useContentStore（本地 mock）
- [x] 新增 `pangen-ai-compass/src/store/useContentStore.ts`：items/assets 初始化（storageGet + guard）（≤50 行）
- [x] 增加 createDraft/updateDraft（≤50 行）
- [x] 增加 publish/unpublish/delete（≤50 行）
- [x] 增加 upsertAsset（≤50 行）
- [x] 增加 selector：getPublishedArticlesByDomain/getArticleById（≤50 行）

## C. Admin 信息架构：内容管理入口与路由
- [x] 在 `pangen-ai-compass/src/components/AdminLayout.tsx` NAV_ITEMS 增加“内容管理”（≤30 行）
- [x] 在 `pangen-ai-compass/src/routes.tsx` 为 admin children 增加 content 路由（≤50 行）

## D. 内容列表页（/admin/content）
- [x] 新增 `pangen-ai-compass/src/pages/admin/ContentListPage.tsx` 页面骨架（≤50 行）
- [x] 增加筛选：状态/领域/可见性/关键词（≤50 行）
- [x] 列表字段：封面/标题/状态/领域/可见性/更新时间（≤50 行）
- [x] 行内操作：编辑/发布/下架/删除（≤50 行）
- [x] 新建入口：跳转 /admin/content/new（≤30 行）

## E. 编辑器页结构（/admin/content/new & /admin/content/:id/edit）
- [x] 新增 `pangen-ai-compass/src/pages/admin/ContentEditorPage.tsx`：读取内容与基础表单（≤50 行）
- [x] 三栏布局骨架：大纲/编辑/预览（≤50 行）
- [x] 元信息表单：标题/摘要/封面URL/领域/可见性/学校（≤50 行）
- [x] 发布按钮：调用 publish（≤30 行）
- [x] 下架按钮：调用 unpublish（≤30 行）

## F. Obsidian 编辑器内核：CodeMirror 与工具栏
- [x] 安装依赖：CodeMirror Markdown 相关包（≤50 行变更以 package.json 为准）
- [x] 新增 `pangen-ai-compass/src/components/MarkdownEditor.tsx`：CodeMirror 最小接入（≤50 行）
- [x] 工具栏：加粗/斜体/链接/代码块/H1-H3（≤50 行）
- [x] 快捷键：Ctrl+S 保存草稿（≤50 行）

## G. 自动保存（每 2 秒保存草稿）
- [x] 在 ContentEditorPage 增加 dirty 标记与 2 秒保存节流（≤50 行）
- [x] 底部状态条：显示“保存中/已保存时间/字数统计”（≤50 行）

## H. 图片上传（拖拽/粘贴）
- [x] 在 MarkdownEditor 接入 paste/drop 读取图片（≤50 行）
- [x] 限制图片大小并写入 assets（≤50 行）
- [x] 插入 Markdown 图片语法到光标处（≤50 行）

## I. 前台内容源替换与受众隔离
- [x] 在 `pangen-ai-compass/src/pages/HomePage.tsx` 使用 useContentStore 已发布文章替换 MOCK_ARTICLES（≤50 行）
- [x] 在 `pangen-ai-compass/src/pages/ArticleReadPage.tsx` 按 content store 查找文章（≤50 行）
- [x] 抽取统一 access policy：对 campus 内容做列表过滤与详情拦截复用（≤50 行）

## J. 校内与领域的组合展示一致性
- [x] 校内专区页（CampusPage）补齐：从 content store 读取校内文章（≤50 行）
- [x] 首页与校内专区的 domain 切换后内容同步一致（≤50 行）

## K. 回归与自检（人工 + 命令）
- [x] 内容：新建草稿→输入→自动保存→刷新不丢
- [x] 发布：草稿发布后前台可见；下架后前台不可见
- [x] 校内：校内内容仅本校已认证可见；直链未认证被拦截
- [x] 编辑器：大纲点击定位、预览实时更新、快捷键保存可用
- [x] 运行 `pnpm -C pangen-ai-compass lint`

## L. 规格归档与同步（Specs/PRD）
- [x] 在 `openspec/Specs/` 新增：`Spec-内容管理-Obsidian编辑器与发布流.md`（对齐实现，写全细节）
- [x] 在 `openspec/Specs/` 新增：`Spec-受众分层-公开与校内隔离.md`（对齐实现，写全细节）
- [x] 按 `04-规格增量.md` 将增量同步到 PRD（用户后台/系统后台/内容管理优先级）
- [x] 更新 `openspec/Specs/主规格.md` 索引（必要时）
