# 执行清单（Atomic Checklist）

规则：
- 每项任务为原子化改动，单项代码改动 ≤50 行。
- 标记 `【可并行】` 的任务可同时进行。
- 完成后用 `[x]` 勾选。

## A. 游客额度（3+3/天）与演示模式
- [ ] 【可并行】在 `src/utils/storage.ts` 增加游客额度相关 STORAGE_KEYS（≤20 行）
- [ ] 【可并行】新增 `src/utils/quota.ts`：读取/扣减/重置（本机 00:00）工具函数（≤50 行）
- [ ] 【可并行】为 quota 增加数据校验 guard（≤40 行）
- [ ] 在 `AISearch` 触发 AI 搜索前接入“扣次→不足则 demo”逻辑（≤50 行）
- [ ] 在 `SolutionNewPage` 触发 AI 生成前接入“扣次→不足则 demo”逻辑（≤50 行）
- [ ] 【可并行】在首页搜索框附近展示额度徽标：`AI 搜索 x/3 · 方案 y/3`（≤50 行）
- [ ] 【可并行】当 demo 原因来自“额度耗尽”时，提示条文案补充“今日额度已用完”说明（≤40 行）

## B. 文档页：混排视频块 + 目录
- [ ] 【可并行】定义受控块语法常量（video/callout）与解析入口（≤50 行）
- [ ] 【可并行】新增 Markdown 渲染组件 `DocRenderer`（封装 react-markdown + 插件）（≤50 行）
- [ ] 【可并行】实现 `:::video{...}:::` 的渲染（HTML5 video，失败提示） （≤50 行）
- [ ] 【可并行】实现 `:::callout{...}...:::` 的渲染（Obsidian 风格样式） （≤50 行）
- [ ] 在 `ArticleReadPage` 替换为 `DocRenderer`（≤50 行）
- [ ] 【可并行】实现从 Markdown 提取 H1–H6 并生成 TOC 数据（≤50 行）
- [ ] 在桌面右侧 TOC 用真实数据替换占位（≤50 行）
- [ ] 移动端增加 TOC 抽屉入口（≤50 行）

## C. 双链（doc/tool/solution）基础渲染（不含反向链接）
- [ ] 【可并行】实现 `[[...]]` 语法解析（doc/tool/solution）并转为链接节点（≤50 行）
- [ ] 在文档预览渲染中启用双链渲染（≤50 行）
- [ ] 工具/方案不存在时展示“未找到”提示样式（≤40 行）

## D. 后台骨架与路由
- [ ] 【可并行】新增后台路由结构（review-queue/review-detail/unassigned/reviewers/audit）占位页（每页 ≤50 行）
- [ ] 将 `/admin` 从单页占位替换为后台布局（侧边栏 + 内容区）（≤50 行）

## E. 审核任务模型（本地存储原型）
- [ ] 【可并行】定义 ReviewTask 类型与状态枚举（≤50 行）
- [ ] 【可并行】定义 unassignedReason 枚举与中文文案映射（≤50 行）
- [ ] 【可并行】在 localStorage 增加 reviewTasks 存储键与读写工具函数（≤50 行）

## F. 自动分配（WIP=10, SLA=48h, 修订优先同人）
- [ ] 【可并行】定义 ReviewerProfile（可审领域、接单开关）数据模型（≤50 行）
- [ ] 【可并行】实现“候选池过滤 + 最小负载 + 轮转”分配函数（≤50 行）
- [ ] 当新稿件进入 pending 时触发 auto_assign（本地模拟入口先在后台按钮触发）（≤50 行）
- [ ] 满载进入待分配池，并写入 unassignedReason=all_reviewers_over_capacity（≤50 行）

## G. 待分配池（严格不超配）
- [ ] 待分配池页面展示：原因、建议动作、手动指派入口（≤50 行）
- [ ] 自动恢复：当释放容量/恢复接单时尝试重新分配一条（≤50 行）

## H. 改派与权限（editor 可改派）
- [ ] 【可并行】定义角色枚举与权限判断工具函数（≤50 行）
- [ ] 队列页：editor/admin 显示“更改指派”按钮（≤50 行）
- [ ] 详情页：in_review 状态禁止 editor 改派；admin 可强制改派并必填原因（≤50 行）

## I. 审计日志
- [ ] 【可并行】定义 AuditLog 类型与 action/reasonCode 枚举（≤50 行）
- [ ] 在 auto_assign/manual_assign/reassign/pause/resume 时写审计（≤50 行）
- [ ] 审计日志列表页（只读） （≤50 行）

## J. 审核详情页 AI 助手（只后台，Diff + 一键填入）
- [ ] 【可并行】审核详情页右侧增加 AI 助手侧栏布局（≤50 行）
- [ ] 【可并行】实现“拒绝原因草稿（严谨模板）”按钮的 UI 形态与填入逻辑（≤50 行）
- [ ] 【可并行】实现“快速审读摘要”按钮（≤50 行）
- [ ] 【可并行】实现“修订稿改动总结（二审）”按钮（≤50 行）
- [ ] 应用 AI 建议后自动生成 revision 记录（≤50 行）

## K. 移动端像 App（基础适配）
- [ ] 【可并行】底部 Tab 导航骨架（首页/方案/我的） （≤50 行）
- [ ] 【可并行】移动端 Dock 改为底部 Sheet（避免遮挡 Tab 与键盘）（≤50 行）
- [ ] Safe Area 与键盘遮挡回归检查（人工验收项）

## L. 回归与自检（人工）
- [ ] 游客额度：搜索 3 次/天、方案 3 次/天；第 4 次自动 demo；00:00 重置
- [ ] 文档页：多视频块渲染、目录跳转、移动端 TOC 抽屉
- [ ] 后台：队列→详情→拒绝/通过→审计；满载进入待分配池
