# V2 变更包（盘根 · AI 指南针）执行清单：可维护性增强

## 0. 执行原则

- 每一项任务改动控制在 50 行以内
- 每一项任务完成后立即自检：类型通过、页面可编译
- 不改变用户可见行为

## 1. 原子任务列表

- [ ] T1：建立根目录宪法目录并落地 TechStack
  - 改动文件：新增 `openspec/Constitution/TechStack.md`
  - 验收标准：根目录存在 `openspec/Constitution/`，TechStack 内容完整

- [ ] T2：清理根目录无效锁文件
  - 改动文件：删除仓库根目录 `package-lock.json`
  - 验收标准：仓库根目录无 `package-lock.json`

- [ ] T3：新增 AI 契约类型 `AISearchResultV2`
  - 改动文件：新增 `1pangen-ai-compass/services/aiContract.ts`
  - 验收标准：类型包含 `mode` 与 `fallbackReason` 字段

- [ ] T4：新增 AI 回退生成器模块
  - 改动文件：新增 `1pangen-ai-compass/services/aiFallback.ts`
  - 验收标准：回退输出工具数量上限 3，内容数量上限 2，`summary` 包含“演示模式”

- [ ] T5：改造 `geminiService.ts`，统一输出 `AISearchResultV2`
  - 改动文件：修改 `1pangen-ai-compass/services/geminiService.ts`
  - 验收标准：缺少密钥时返回 `mode = demo`；异常时返回 `mode = demo`；正常成功返回 `mode = ai`

- [ ] T6：改造 `AISearch.tsx`，演示模式判断改为显式字段
  - 改动文件：修改 `1pangen-ai-compass/components/AISearch.tsx`
  - 验收标准：提示条展示条件为 `result.mode === 'demo'`

- [ ] T7：新增本地存储工具模块
  - 改动文件：新增 `1pangen-ai-compass/utils/storage.ts`
  - 验收标准：集中键名与读写函数，包含安全解析与校验

- [ ] T8：新增类型守卫模块
  - 改动文件：新增 `1pangen-ai-compass/utils/guards.ts`
  - 验收标准：包含 `isStringArray`、`isUserSolutionArray` 等纯函数

- [ ] T9：改造 `App.tsx` 使用存储模块
  - 改动文件：修改 `1pangen-ai-compass/App.tsx`
  - 验收标准：`App.tsx` 不再直接访问本地存储键名常量与 JSON 解析函数

- [ ] T10：回写主规格
  - 改动文件：修改 `openspec/Specs/PRD-盘根AI指南针-最终版.md`
  - 验收标准：AI 输出字段新增 `mode`、`fallbackReason` 并定义取值

- [ ] T11：存档提交
  - 改动文件：无
  - 验收标准：git commit 信息包含 V2 可维护性增强范围

