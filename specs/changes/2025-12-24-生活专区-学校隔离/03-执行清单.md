# 执行清单：生活专区（学校隔离）

## 阶段一：数据模型与配置

### 1.1 类型定义
- [ ] `src/types.ts`：Domain 类型增加 `'life'`

### 1.2 领域配置常量
- [ ] 新建 `src/constants/domains.ts`
- [ ] 导出 `DOMAIN_CONFIG` 对象（4 个领域的配置）
- [ ] 导出 `DOMAIN_LIST` 数组

### 1.3 Content Store
- [ ] `src/store/useContentStore.ts`：`DOMAIN_FOLDER_MAP` 增加 `life: '/生活'`
- [ ] `SEED_FOLDERS` 增加生活专区种子文件夹

## 阶段二：访问控制

### 2.1 访问控制函数
- [ ] `src/utils/access.ts`：新增 `canAccessDomain(domain, cert)` 函数
- [ ] `src/utils/access.ts`：重构 `canAccessContent()` 支持三层控制
- [ ] 增加类型导入 `DOMAIN_CONFIG`

## 阶段三：UI 组件

### 3.1 领域切换下拉
- [ ] `src/components/AppLayout.tsx`：从 `DOMAIN_CONFIG` 渲染领域列表
- [ ] 增加分隔线（life 之前）
- [ ] life 领域未认证时显示锁图标
- [ ] 点击 life 时允许切换（不拦截，Empty State 处理）

### 3.2 顶栏标题
- [ ] life 领域时显示学校名而非领域名
- [ ] 格式：`盘根 AI · {schoolName}`

### 3.3 Empty State 组件
- [ ] 新建 `src/components/LifeEmptyState.tsx`
- [ ] 插画 + 文案 + 认证按钮

## 阶段四：首页逻辑

### 4.1 HomePage 适配
- [ ] life 领域 + 未认证 → 显示 Empty State
- [ ] life 领域 + 已认证 → 过滤本校内容
- [ ] 其他领域保持现有逻辑

## 阶段五：Mock 数据（可选）

### 5.1 生活专区种子内容
- [ ] `src/constants.ts`：增加 life 领域的 mock 工具/文章
- [ ] 确保 mock 内容带有 `schoolId`
