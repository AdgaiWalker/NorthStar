# 当前现状交接

> 更新日期：2026-04-28
> 用途：切换新对话后继续全量 PRD 开发的事实基线。
> 判断原则：以当前代码、提交、测试和本地验证为准，不按旧计划文案假设完成度。

## 1. 当前 Git 状态

- 当前分支：`main`
- 当前 HEAD：`5833eff`
- 生成本文档前工作区：存在本轮差距修复未提交改动
- 最近关键提交：
  - `5833eff`：当前本地 HEAD（Consolidate platform planning around final target architecture）
  - `1d43510`：校园搜索改为 `search_documents` 索引、全文搜索、搜索缺口报表
  - `a096f89`：我的页下一步能力、AI 写作内容标注
  - `a230708`：有权限用户创建校园空间
  - `cf3653f`：校园举报前端切到 `/api/moderation/tasks`
  - `e807476`：P0 平台闭环，补 `identity / compliance / moderation / admin-console`
  - `6a9cd49`：统一平台地基

## 2. 当前产品形态

当前代码已经从“校园站单产品”推进到“盘根统一平台起点”：

```text
frontlife-web      校园站用户前台
frontai-web        全球站用户前台
admin-console      统一中台后台
server             统一模块化后端
@ns/shared         统一类型和契约包
PostgreSQL         当前开发期单库
```

默认产品规则已经固定：

- 中文文案优先。
- 早期注册采用用户名 + 密码。
- 邮箱、手机号作为后置绑定能力；不做手机号 / SMS 注册分支。
- UI 图标继续只用 Lucide。
- 没有显性要求，不写长期兼容代码。
- 新平台接口优先走命名空间：`/api/identity/*`、`/api/compliance/*`、`/api/moderation/*`、`/api/admin/*`、`/api/campus/*` 等。
- 旧接口不新增桥接层；页面切到新接口并有 smoke 后再删旧路径。

## 3. 本地运行入口

常用端口：

- `server`：`http://localhost:4000`
- `frontai-web`：`http://localhost:3000`
- `frontlife-web`：`http://localhost:3001`
- `admin-console`：`http://localhost:3002`

常用命令：

```bash
cd NorthStar/packages/server
pnpm db:push
pnpm db:seed
pnpm start
```

```bash
cd NorthStar/packages/frontlife-web
pnpm dev -- --host 0.0.0.0 --port 3001
```

```bash
cd NorthStar/packages/frontai-web
pnpm dev -- --host 0.0.0.0 --port 3000
```

```bash
cd NorthStar/packages/admin-console
pnpm dev -- --host 0.0.0.0 --port 3002
```

seed 后可用账号：

- 校园普通用户：`zhang / password`
- 校园编辑：`editor / password`
- 后台管理员：`admin / password`

## 4. 最近验证结果

2026-04-28 全量验证已通过：

- `shared`：`npx tsc --noEmit`
- `server`：`npx tsc --noEmit` + `pnpm test`，36/36 tests 通过
- `frontlife-web`：`npx tsc --noEmit` + `pnpm build`
- `frontai-web`：`npx tsc --noEmit` + `pnpm build`
- `admin-console`：`pnpm typecheck` + `pnpm build`

2026-04-28 Chrome 端到端测试通过（两站核心流程）：

- 校园站（localhost:3001）：首页、搜索、文章详情、登录、个人中心、设置
- 全球站（localhost:3000）：首页、工具详情、注册、登录、AI 搜索、方案生成、用户中心

已修复 UI 问题：

- 首页工具只显示 1 个（被 domain 过滤）→ 已修复
- 用户中心 "My User" / "Pro Member" 英文占位 → 已修复
- 文章页 "帮助了 6191 人" 文案歧义 → 已修复为"累计帮助"

已完成差距修复（A1-A3、B1、C1-C2）：

- A1：Admin 内容详情查看 + 状态变更（ContentDetailPage + 路由）
- A2：Admin 列表筛选 + 全列表分页（Pagination 组件 + 5 个列表页）
- A3：Admin 审计日志增强（before/after 展开 + 操作者 + action 筛选）
- B1：校园站页脚 + 法律链接（Footer 组件 + PageLayout 接入）
- C1+C2：文档同步与归档

下一步可做：

- 提交所有改动，打 release tag
- 部署到测试环境
- 接入真实 OAuth / 邮件 / 支付 provider

## 5. 已完成的平台能力

### 5.1 平台地基

已完成：

- `packages/admin-console` 已建立。
- `server/src/modules` 已建立模块目录：
  - `platform`
  - `identity`
  - `campus`
  - `compass`
  - `moderation`
  - `ai-gateway`
  - `notification`
  - `analytics`
  - `billing`
  - `compliance`
- `SiteContext = cn | com | all` 已进入 shared/server。
- `site_configs`、`audit_logs`、`moderation_tasks` 已落库。
- 新平台接口已经有统一 envelope 工具：`{ ok: true, data } / { ok: false, error }`。
- 非 admin 不允许 `site=all` 的基础边界已测。
- `GET /api/platform/capabilities?site=campus|compass` 已提供站点能力结果。

### 5.2 Identity

已完成：

- `/api/identity/register`
- `/api/identity/login`
- `/api/identity/me`
- `/api/identity/email/verify`
- `/api/identity/password-reset/request`
- `/api/identity/password-reset/confirm`
- 用户名或邮箱登录。
- 用户名、邮箱唯一。
- 密码 hash 存储。
- JWT session。
- 登录态按 `site` 隔离。
- `tokenInvalidBefore` 已作为 token 失效机制。
- 重置密码、注销完成、禁用账号后可使旧 token 失效。
- 角色口径已固定为：
  - `user`
  - `editor`
  - `reviewer`
  - `operator`
  - `admin`

注意：

- 旧 `/api/auth/*` 仍存在，校园站主登录页还未完全切到 `/api/identity/*`。

### 5.3 Compliance

已完成：

- `legal_documents`
- `user_consents`
- `account_deletion_requests`
- 协议/隐私政策公开查询 API。
- 注册时可写入 consent version。
- 本人数据导出 API。
- 注销申请 API。
- 后台处理注销申请后旧 token 失效。
- seed 写入 `cn/com` 默认协议和隐私政策。

注意：

- 前台协议/隐私政策页面入口尚未完整接入。
- 数据删除任务只是最小闭环，还没有做异步任务和 GDPR 全量流程。

### 5.4 Moderation

已完成：

- `moderation_tasks` 表。
- 审核任务创建。
- 审核队列查询。
- 审核详情查询。
- 状态流转：
  - `pending -> in_review -> resolved`
  - `pending -> in_review -> dismissed`
  - `pending -> escalated -> resolved`
- 状态变化写 `audit_logs`。
- 普通用户可提交任务，但不可查看/处理任务。
- `reviewer/operator/admin` 可查看和处理任务。
- 校园举报已进入 `/api/moderation/tasks`。
- 校园“有变化”反馈会创建 moderation task。

注意：

- AI 输出抽检尚未进入 moderation。
- 全球站申请审核尚未进入 moderation。
- 全球站内容审核尚未进入 moderation。

### 5.5 Admin Console

已完成：

- 真实登录页：用户名/邮箱 + 密码。
- 登录后校验 role，只允许 `reviewer/operator/admin`。
- Site switcher：`cn/com/all`，`all` 只给 admin。
- Dashboard 总览。
- ReviewQueue。
- ReviewDetail。
- 用户管理。
- 内容管理：当前主要接校园文章/帖子基础列表。
- 审计日志。
- 系统配置查看/编辑。
- 审核详情已展开空间认领 payload，包含空间、候选人和最后活跃时间。
- 数据中心、支付管理目前接 health 状态，不是完整业务页。
- 所有页面已有中文加载态、空态、错误态。
- 所有图标使用 Lucide。

## 6. 校园站当前状态

已完成或基本完成：

- 首页、搜索页、探索页、空间页、文章页、我的页、登录页。
- 错误态和空态基础收口。
- 失效 token 的前端统一处理。
- 发帖、回复、标记解决。
- 文章阅读、反馈、收藏、通知。
- AI 写文章 fallback。
- 举报主路径已切到 `/api/moderation/tasks`。
- “有变化”会同步创建 moderation task。
- 有权限用户可通过 `/api/campus/spaces` 创建空间。
- editor/admin 或高信任用户可创建空间。
- 我的页显示“下一步能力”，不再展示等级数字。
- AI 写作发布内容会追加“AI 辅助”标注。
- `search_documents` 已建立，seed 自动刷新索引。
- `pnpm search:refresh` 可手动刷新搜索索引。
- 搜索已走 PostgreSQL 全文搜索 + 标题精确/部分匹配排序。
- 搜索页已形成 exact / partial / empty 三态；empty 可转求助帖，并预填“我想问：{query}”。
- 搜索缺口报表：`/api/search/gaps`，仅后台角色可访问。
- 空间认领已支持 90 天无人维护扫描、`space_claim` moderation task、claim 通知、后台审核通过后更新 owner / maintainer。
- 通知类型枚举已固定为 `auth_invite`、`feedback`、`changed`、`expiry`、`claim`、`reply`，其中反馈、变化、认领、回复已有真实触发或测试覆盖，认证邀请和过期通知为 service-level 创建能力。

仍需注意：

- 校园登录页已完全切到 `/api/identity/*`。
- 校园我的页已接数据导出、注销入口。
- 法律页面入口已通过页脚 Footer 组件暴露给用户。
- 旧 API 路径已全部迁移完毕，无 `/api/spaces`、`/api/auth` 等残留。

## 7. 全球站当前状态

已有：

- 全球站登录/注册主路径已接 `/api/identity/*`，site 固定为 `com`。
- 全球站首页、工具详情、文章阅读已接 `/api/compass/tools|topics|articles|news`。
- `/solution/new` 支持目标优先，不要求先选工具；生成后保存到后端 `solutions`。
- 用户中心“我的方案”已接真实后端列表、删除、导出。
- 后端已提供 `solutions`、`solution_feedbacks`、`solution_exports`，并验证 owner 隔离、导出、反馈。
- 前端 `/admin` 的明显 placeholder 入口已改成 Locked/迁移提示或跳转，避免假页面冒充真实后台。
- `/api/ai/tools` fallback 链路已可用。
- 前端构建、类型检查、lint 通过。

主要缺口：

- 申请制、邀请码已完成。GitHub OAuth 后端已实现，前端显示不可用态，等 OAuth App 配置后接入。
- 全球站收藏已接真实 API。
- 额度、支付已完成。数据导出/删除已完成。
- 内容工作室不做独立 UI，前台内容创建走后端 API。

## 8. 数据库与 seed 当前状态

seed 当前会重置并生成：

- 8 个校园空间
- 59 篇校园文章
- 2 条帖子
- `cn/com` 默认协议和隐私政策
- 后台管理员 seed 账号
- `search_documents` seed 后 70 条，`search:refresh` 后当前 88 条

关键新增表：

- `site_configs`
- `audit_logs`
- `moderation_tasks`
- `legal_documents`
- `user_consents`
- `account_deletion_requests`
- `search_documents`
- `content_records`
- `content_versions`
- `solutions`
- `solution_feedbacks`
- `solution_exports`

## 9. 重要文件索引

平台/后端：

- `NorthStar/packages/server/src/index.ts`
- `NorthStar/packages/server/src/db/schema.ts`
- `NorthStar/packages/server/src/lib/http.ts`
- `NorthStar/packages/server/src/middleware/auth.ts`
- `NorthStar/packages/server/src/middleware/site.ts`
- `NorthStar/packages/server/src/modules/identity/*`
- `NorthStar/packages/server/src/modules/compliance/*`
- `NorthStar/packages/server/src/modules/moderation/*`
- `NorthStar/packages/server/src/modules/platform/*`
- `NorthStar/packages/server/src/modules/campus/*`
- `NorthStar/packages/server/src/data/postgres.ts`
- `NorthStar/packages/server/src/scripts/seed.ts`
- `NorthStar/packages/server/src/scripts/refresh-search-documents.ts`

前端：

- `NorthStar/packages/frontlife-web/src/services/api.ts`
- `NorthStar/packages/frontlife-web/src/pages/ExplorePage.tsx`
- `NorthStar/packages/frontlife-web/src/pages/SpacePage.tsx`
- `NorthStar/packages/frontlife-web/src/pages/ProfilePage.tsx`
- `NorthStar/packages/admin-console/src/App.tsx`
- `NorthStar/packages/admin-console/src/services/api.ts`

shared：

- `NorthStar/packages/shared/src/site.ts`
- `NorthStar/packages/shared/src/api-envelope.ts`
- `NorthStar/packages/shared/src/admin-contract.ts`
- `NorthStar/packages/shared/src/identity-contract.ts`
- `NorthStar/packages/shared/src/compliance-contract.ts`
- `NorthStar/packages/shared/src/moderation-contract.ts`
- `NorthStar/packages/shared/src/campus-contract.ts`

规格：

- `specs/全量任务计划.md`

## 10. 新对话接手建议

新对话不要重新讨论架构，直接按剩余任务推进。

参考文档：

- `specs/差距分析与待办.md` — 已确认完成项和暂缓项
- `specs/差距修复执行清单.md` — 已全部完成的 A1-A3、B1、C1-C2 清单
- `specs/全量补齐执行清单.md` — P0-P7 全量执行清单
- `specs/后续未完成任务.md` — 已归档，不再维护

建议第一句可以写：

```text
继续按 specs/当前现状.md 接手开发。
当前所有差距修复已完成，下一步是提交改动、打 tag、接入真实 provider。
规则：中文文案；不写手机号/SMS；不写长期兼容层；Lucide 图标；新接口走命名空间。
```
