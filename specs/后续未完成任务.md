# 后续未完成任务

> **已归档**（2026-04-28）
> 本文档已被 `specs/全量补齐执行清单.md` 和 `specs/差距分析与待办.md` 替代。
> 本文档中的大部分条目已由 P0-P7 执行完成，剩余待办已迁移到差距分析文档。
> 不再维护此文件。

> 生成日期：2026-04-25
> 用途：新对话继续全量 PRD 开发时使用。
> 执行规则：中文文案；不做手机号/SMS；不写长期兼容层；UI 图标只用 Lucide；新接口走命名空间；遇到小问题按统一平台原则自行决定。

## 0. 总体优先级

当前不建议直接去做发布 tag。还没有全量完成。

推荐继续执行顺序：

1. 校园站剩余闭环：账号合规前端、空间认领、通知 6 类型、搜索无结果转求助。
2. 全球站真实化：真实账号、申请/邀请、内容模型、方案闭环、用户后台。
3. 横向能力：AI 网关、analytics、notification/email、billing。
4. 删除旧路径：旧校园 API 调用、全球 mock 登录、后台 placeholder、长期 mock。
5. 全量验证、文档、发布。

## 1. P0：接手前必须确认

- [ ] `git status --short` 必须干净，或先解释未提交改动。
- [ ] 确认当前 HEAD 至少包含：
  - `1d43510 Index campus search through searchable documents`
  - `a096f89 Clarify campus growth and AI-assisted writing states`
  - `a230708 Let qualified campus users create spaces`
  - `cf3653f Move campus reporting onto the moderation namespace`
  - `e807476 Close the first usable platform loop before feature expansion`
- [ ] 跑最小验证：
  - `cd NorthStar/packages/server && npx tsc --noEmit`
  - `cd NorthStar/packages/server && pnpm test`
  - `cd NorthStar/packages/server && pnpm db:push`
  - `cd NorthStar/packages/server && pnpm db:seed`
  - `cd NorthStar/packages/frontlife-web && npx tsc --noEmit`
  - `cd NorthStar/packages/frontlife-web && pnpm test`
  - `cd NorthStar/packages/admin-console && pnpm typecheck`
- [ ] 只要改了 shared 契约，必须跑三个前端 typecheck/build。

## 2. P1：校园站剩余闭环

### 2.1 校园账号与合规前端接入

现状：

- 后端 `/api/identity/*` 已有。
- 后端 `/api/compliance/*` 已有。
- 校园站旧 `/api/auth/*` 仍在使用。

任务：

- [ ] 校园登录页注册改为 `/api/identity/register`。
- [ ] 校园登录页登录改为 `/api/identity/login`。
- [ ] 注册表单增加协议/隐私政策 consent version。
- [ ] 登录后拉 `/api/identity/me`，同步 role/site/emailVerified。
- [ ] 去掉校园站旧 auth 主路径调用。
- [ ] 我的页增加“数据导出”入口，调用 `/api/compliance/data-export`。
- [ ] 我的页增加“注销账号”入口，调用 `/api/compliance/account-deletions`。
- [ ] 增加协议页和隐私政策页，读取 `/api/compliance/legal-documents`。
- [ ] 保留中文错误态、空态、加载态。

验收：

- [ ] 用户名注册成功。
- [ ] 邮箱登录成功。
- [ ] 用户名登录成功。
- [ ] 数据导出返回本人数据。
- [ ] 注销申请进入后台。
- [ ] 后台完成注销后旧 token 失效。

### 2.2 空间认领流程

现状：

- 有权限用户已经能创建空间。
- `moderation_tasks` 支持 `space_claim` 类型。
- `createSpaceClaimNotificationInDb` 已存在，但不是完整流程。

任务：

- [ ] 定义认领触发规则：空间 `updatedAt` 超过 90 天未维护。
- [ ] 新增脚本或 API：扫描长期无人维护空间并创建 `space_claim` moderation task。
- [ ] 认领任务 payload 带 `spaceId/spaceSlug/currentOwnerId/candidateUserId`。
- [ ] 认领邀请触发通知。
- [ ] admin-console ReviewDetail 能展示 `space_claim` payload 的核心信息。
- [ ] 审核通过后更新 `knowledge_bases.ownerId/claimedBy/isClaimed`。
- [ ] 审核驳回后保留审计记录。

验收：

- [ ] 90 天无人维护空间能生成认领任务。
- [ ] 后台可处理认领。
- [ ] 认领通过后空间维护者变化。
- [ ] 操作写入 `audit_logs`。

### 2.3 通知 6 类型补齐

当前通知已有部分触发，但 PRD 口径还没完全锁死。

任务：

- [ ] 认证邀请：达到条件后真实触发。
- [ ] 内容反馈：有帮助、收藏等反馈真实触发。
- [ ] 变动提醒：“有变化”审核处理后通知相关维护者。
- [ ] 过期提醒：长期未确认文章触发。
- [ ] 空间认领邀请：认领任务生成后触发。
- [ ] 帖子回复：已有基础触发，补测试锁住。
- [ ] 顶栏未读数和我的页未读数保持同步。
- [ ] 点击通知后两处同步减少。
- [ ] 移动端只展示最近 12 条，不撑爆页面。

验收：

- [ ] 每种通知都有真实触发点。
- [ ] 每种通知都有测试或 smoke。
- [ ] 已读状态在顶栏和我的页同步。

### 2.4 搜索无结果转求助

现状：

- `search_documents` 已完成。
- 全文搜索已完成。
- 搜索缺口报表已完成。
- 无结果 AI 兜底前端已有基础。

任务：

- [ ] 搜索无结果页增加“发起求助”入口。
- [ ] 点击后带 query 进入发帖表单，tag 默认 `help`。
- [ ] 提交后创建求助帖。
- [ ] 搜索日志继续记录 resultCount=0、usedAi=true/false。
- [ ] 后台搜索缺口报表能看到高频无结果 query。

验收：

- [ ] 搜索无结果不是死路。
- [ ] 一次无结果搜索可以转为求助帖。
- [ ] 求助帖在空间/动态里可见。

### 2.5 校园内容质量

任务：

- [ ] 补新生到校 7 天内容包。
- [ ] 补报到、食堂、宿舍、选课、快递、网络、二手更真实内容。
- [ ] 清理明显测试标题、乱码、占位文案。
- [ ] 首页推荐卡片继续改高频真实需求。
- [ ] seed 后搜索常见问题能命中。

验收：

- [ ] 首页不像测试环境。
- [ ] 空间页不像测试环境。
- [ ] 文章页不像测试环境。
- [ ] `pnpm db:seed` 可复现。

## 3. P1：全球站真实化

### 3.1 全球账号与准入

现状：

- 后端 identity 可支持 `site=com`。
- 全球站仍有 mock/模拟登录主路径。
- 申请制、邀请制、GitHub OAuth 未完成。

任务：

- [ ] 全球站登录页接 `/api/identity/login`。
- [ ] 全球站注册页接 `/api/identity/register` 或申请制流程。
- [ ] 删除模拟登录主路径。
- [ ] 新增 `application_requests` 表。
- [ ] 新增 `invite_codes` 表。
- [ ] 申请制注册：申请进入 moderation。
- [ ] 邀请码注册：邀请码可用、可失效、可限制次数。
- [ ] GitHub OAuth：provider 未配置时返回中文不可用提示，不阻塞申请/邀请码。
- [ ] 申请通过后通过 dev mail delivery 或邮件通知。

验收：

- [ ] 全球站不依赖 mock 登录。
- [ ] 申请进入后台审核队列。
- [ ] 邀请码注册可用。
- [ ] OAuth 未配置时页面不白屏。

### 3.2 全球内容模型

推荐默认方案：

- 先用统一 `content_records`。
- 用 `contentType=tool/topic/article/news` 区分。
- 不先拆多个内容表，避免重复 CRUD。

任务：

- [ ] 新增 `content_records`。
- [ ] 新增 `content_versions`。
- [ ] 新增工具、专题、文章、资讯 seed。
- [ ] 工具列表/详情 API。
- [ ] 专题列表/详情 API。
- [ ] 文章列表/详情 API。
- [ ] 资讯列表/详情 API。
- [ ] 内容工作室真实 CRUD。
- [ ] 内容发布进入 moderation。
- [ ] admin-console 工具管理和内容管理去 placeholder。

验收：

- [ ] 全球站页面不再只读本地 mock。
- [ ] 后台可管理内容。
- [ ] 发布需要审核。
- [ ] 内容版本可追溯。

### 3.3 全球搜索

任务：

- [ ] 普通搜索覆盖工具、专题、文章、资讯。
- [ ] 普通搜索不足时调用统一 AI 网关。
- [ ] AI 结果有明确标注。
- [ ] 搜索行为写 analytics。
- [ ] 无 AI key fallback 不白屏。

验收：

- [ ] 全球站普通搜索可用。
- [ ] AI fallback 可用。
- [ ] 无结果有可读路径。

### 3.4 方案闭环

任务：

- [ ] 新增 `solutions`。
- [ ] 新增 `solution_exports`。
- [ ] 方案生成接 `/api/compass/solutions`。
- [ ] 保存方案。
- [ ] 方案列表。
- [ ] 方案详情。
- [ ] 导出 md。
- [ ] 导出 txt。
- [ ] 导出 csv。
- [ ] 有效反馈。
- [ ] 方案保存率写入 analytics。

验收：

- [ ] 方案从生成到保存到导出完整。
- [ ] 用户中心能看到自己的方案。
- [ ] 导出文件内容可读。
- [ ] 指标可查询。

### 3.5 全球用户后台

任务：

- [ ] 我的方案。
- [ ] 收藏夹。
- [ ] 我的额度。
- [ ] 个人资料。
- [ ] 设置。
- [ ] 数据导出。
- [ ] 数据删除。
- [ ] 所有空态、错误态、加载态中文完整。

验收：

- [ ] 用户后台全部接真实 API。
- [ ] 无 placeholder。
- [ ] 空数据态完整。

## 4. P2：横向能力

### 4.1 AI 网关

现状：

- 旧 `/api/ai/search`、`/api/ai/write`、`/api/ai/tools` 可用。
- `ai-gateway` 模块还没有统一接管所有 AI 能力。

任务：

- [ ] 新增 AI call log。
- [ ] 输入敏感词检查统一在后端。
- [ ] 输出敏感词检查。
- [ ] 限流。
- [ ] fallback reason 标准化。
- [ ] 校园 AI search/write 走统一网关。
- [ ] 全球 AI tools/search 走统一网关。
- [ ] 内容质量分析进入 AI 网关。
- [ ] AI 输出抽检进入 moderation。
- [ ] 无 AI key 时两站不白屏。

验收：

- [ ] 无 AI key fallback 通过。
- [ ] 敏感输入不发模型。
- [ ] 敏感输出降级或进入审核。
- [ ] 日志可查。

### 4.2 Analytics

任务：

- [ ] 新增 `behavior_events`。
- [ ] 事件白名单。
- [ ] 校园搜索成功率。
- [ ] AI 兜底占比。
- [ ] 有帮助/有变化统计。
- [ ] 全球方案生成完成率。
- [ ] 方案保存率。
- [ ] 校园到全球转化统计。
- [ ] admin 数据中心展示基础指标。
- [ ] 行为数据 90 天清理脚本。

验收：

- [ ] 指标脚本能跑。
- [ ] 90 天清理可验证。
- [ ] admin 能看基础指标。

### 4.3 Notification / Email

任务：

- [ ] 站内通知统一 API。
- [ ] 邮件 provider 抽象。
- [ ] dev provider 写投递记录，不依赖真实邮件服务。
- [ ] 邮件模板。
- [ ] 投递记录。
- [ ] 失败重试。
- [ ] 后台可查邮件失败。

验收：

- [ ] 站内通知可用。
- [ ] 邮件通知可测试。
- [ ] 投递失败可追踪。

### 4.4 Billing

任务：

- [ ] 新增 `quotas`。
- [ ] AI 调用扣减额度。
- [ ] 新增支付订单。
- [ ] 管理后台订单页。
- [ ] 支付 provider 抽象。
- [ ] provider 未定时只做手动确认支付。
- [ ] 额度扣减只在后端发生，前端不能篡改。

验收：

- [ ] 额度不能被前端篡改。
- [ ] AI 调用能扣减额度。
- [ ] 手动确认支付可走通。

## 5. P3：删除旧路径与发布

### 5.1 删除旧路径

注意：只删除已经被新接口替换且有 smoke 覆盖的路径。

任务：

- [ ] 删除被替换的旧校园 API 调用。
- [ ] 删除全球站模拟登录。
- [ ] 删除后台 placeholder 页面。
- [ ] 删除长期 mock 依赖。
- [ ] 保留开发演示 mock 时必须标注 owner 和删除条件。
- [ ] 删除未使用组件。
- [ ] 删除未使用 store 字段。
- [ ] 删除无 owner 兼容层。

当前不可直接删：

- `/api/spaces`
- `/api/articles`
- `/api/posts`
- `/api/feed`
- `/api/search`
- `/api/me`
- `/api/favorites`
- `/api/notifications`

原因：校园站仍有页面依赖这些旧接口。

### 5.2 全量验证

最终发布前必须跑：

- [ ] `pnpm -r test` 或逐包等价命令。
- [ ] 三个前端 typecheck。
- [ ] 三个前端 build。
- [ ] server typecheck/test。
- [ ] `db:push`。
- [ ] `db:seed`。
- [ ] `pnpm search:refresh`。
- [ ] 校园真实 API smoke。
- [ ] 全球真实 API smoke。
- [ ] admin smoke。
- [ ] 数据隔离专项。
- [ ] 无 AI key fallback。
- [ ] 失效 token。
- [ ] 空数据态。
- [ ] 断 server。
- [ ] 邮箱注册/验证/密码重置专项。
- [ ] 行为数据 90 天清理专项。

### 5.3 文档与发布

任务：

- [ ] README 更新统一平台启动方式。
- [ ] README 增加三前端说明。
- [ ] 写 PRD 对照表。
- [ ] 写 release checklist。
- [ ] 记录包体大小。
- [ ] 记录测试结果。
- [ ] 记录 smoke 结果。
- [ ] Lore commit。
- [ ] push。
- [ ] tag。

注意：

- 只有全量验收通过后才 push/tag。
- commit message 遵守 Lore Commit Protocol。

## 6. 下一步最推荐切片

如果下一轮只能选一个切片，建议做：

```text
校园账号与合规前端接入
```

原因：

- 后端 identity/compliance 已经有最小闭环。
- admin-console 也能处理注销。
- 校园站仍使用旧 `/api/auth/*`，这是当前平台化最明显的断点。
- 做完后，校园站账号、协议、导出、注销才算真正接入统一平台。

建议拆成 4 个提交：

1. 校园登录页切 `/api/identity/login` 和 `/api/identity/register`。
2. 增加协议/隐私政策页面和 consent version。
3. 我的页增加数据导出、注销申请。
4. 后端/前端测试 + checklist 回写。

## 7. 新对话执行提示词

可直接复制：

```text
请根据 specs/当前现状.md 和 specs/后续未完成任务.md 继续全量 PRD 开发。
优先做“校园账号与合规前端接入”。
规则：中文文案；不做手机号/SMS；不写长期兼容层；UI 图标只用 Lucide；新接口走命名空间；遇到小问题自行按统一平台原则处理。
完成后跑 server/frontlife/shared/admin-console 相关验证，并用 Lore 协议提交。
```

