# 执行清单（Atomic Checklist）

规则：
- 每项任务为原子化改动，单项代码改动 ≤50 行。
- 标记 `【可并行】` 的任务可同时进行。
- 完成后用 `[x]` 勾选。

## A. 数据层：存储键 + 类型 + guard
- [x] 【可并行】在 `pangen-ai-compass/src/utils/storage.ts` 增加：favoriteToolIds/defaultExportFormat/studentCertification（≤50 行）
- [x] 【可并行】在 `pangen-ai-compass/src/types.ts` 增加：ExportFormat/CertificationStatus/StudentCertification（≤50 行）
- [x] 【可并行】在 `pangen-ai-compass/src/utils/guards.ts` 增加：isExportFormat/isStudentCertification（≤50 行）

## B. useAppStore：收藏与导出偏好
- [x] 在 `pangen-ai-compass/src/store/useAppStore.ts` 增加 favoriteToolIds 初始化（storageGet + guard）（≤50 行）
- [x] 在 `pangen-ai-compass/src/store/useAppStore.ts` 增加 toggleFavoriteTool/isToolFavorited 并写入 storage（≤50 行）
- [x] 在 `pangen-ai-compass/src/store/useAppStore.ts` 增加 defaultExportFormat 初始化 + setDefaultExportFormat（≤50 行）

## C. useAppStore：学生认证状态机（前端演示）
- [x] 在 `pangen-ai-compass/src/store/useAppStore.ts` 增加 studentCertification 初始化（≤50 行）
- [x] 在 `pangen-ai-compass/src/store/useAppStore.ts` 增加 submitStudentCertification（≤50 行）
- [x] 在 `pangen-ai-compass/src/store/useAppStore.ts` 增加 mockApprove/mockReject/reset（≤50 行）

## D. 收藏功能：入口与列表
- [x] 在 `pangen-ai-compass/src/pages/ToolDetailPage.tsx` 接入收藏按钮状态与点击逻辑（≤50 行）
- [x] 在 `pangen-ai-compass/src/pages/UserCenterPage.tsx` 实现收藏夹列表页（/me/favorites）与空态（≤50 行）
- [x] 在 `pangen-ai-compass/src/pages/UserCenterPage.tsx` 收藏条目支持“加入方案/取消收藏”（≤50 行）

## E. 设置功能：真实可用（即时生效 + 持久化）
- [x] 在 `pangen-ai-compass/src/pages/UserCenterPage.tsx` 实现设置页 UI（主题/语言/默认导出格式）（≤50 行）
- [x] 在 `pangen-ai-compass/src/pages/UserCenterPage.tsx` 将设置项接入 useAppStore（≤50 行）
- [x] 在 `pangen-ai-compass/src/pages/UserCenterPage.tsx` 调整“快捷导出”使用 defaultExportFormat（≤50 行）

## F. 我的额度页（游客 3+3/天可视化）
- [x] 在 `pangen-ai-compass/src/pages/UserCenterPage.tsx` 新增 tab=credits 页面（≤50 行）
- [x] 在额度页接入 `getGuestQuotaState()` 并展示 resetAt（≤50 行）
- [x] 在额度页增加主按钮（去首页 AI 搜索）与次按钮（去生成方案）（≤50 行）

## G. 学生认证页（前端演示）
- [x] 在 `pangen-ai-compass/src/pages/UserCenterPage.tsx` 新增 tab=certification 页面骨架（≤50 行）
- [x] 未登录锁定态：引导去 `/login`（≤50 行）
- [x] status=none/pending/rejected/verified 的 UI 状态渲染（≤50 行）
- [x] 内置学校列表（heihe）与提交动作（≤50 行）
- [x] 演示用“模拟通过/驳回/重置”入口（≤50 行）

## H. 校内专区：数据标记 + 内容过滤 + 访问控制
- [x] 在 `pangen-ai-compass/src/constants.ts` 增加校内工具/内容（visibility=campus, schoolId=heihe）（≤50 行）
- [x] 在 `pangen-ai-compass/src/pages/HomePage.tsx` 对 tools/articles/topics 增加“按校内权限过滤”规则（≤50 行）
- [x] 在 `pangen-ai-compass/src/pages/ToolDetailPage.tsx` 增加校内内容访问控制拦截（≤50 行）
- [x] 在 `pangen-ai-compass/src/pages/ArticleReadPage.tsx` 增加校内内容访问控制拦截（≤50 行）

## I. 校内专区页面与导航
- [x] 新增 `pangen-ai-compass/src/pages/CampusPage.tsx`（锁定态 + 已解锁列表）（≤50 行）
- [x] 在 `pangen-ai-compass/src/routes.tsx` 增加 `/campus` 路由（≤30 行）
- [x] 在 `pangen-ai-compass/src/components/MobileBottomNav.tsx` 增加“校内” Tab 与激活判定（≤50 行）
- [ ] 在 `pangen-ai-compass/src/components/AppLayout.tsx` 增加桌面端校内入口（≤50 行）—— 可选，移动端已覆盖

## J. 语言偏好最小落地（仅覆盖全局导航/用户中心关键文案）
- [x] 新增 `pangen-ai-compass/src/i18n.ts`：字典 + t(key, lang)（≤50 行）
- [x] 在 `MobileBottomNav` 接入 i18n（≤50 行）
- [x] 在 `UserCenterPage` 侧边栏与关键按钮接入 i18n（≤50 行）

## K. 回归与自检（人工 + 命令）
- [x] 收藏：添加/取消/列表/加入方案联动
- [x] 设置：主题切换立即生效；语言切换影响导航；默认导出格式影响快捷导出
- [x] 额度：显示与扣次逻辑一致；00:00 重置时间显示正确；demo 不扣次说明清晰
- [x] 学生认证：提交→pending→模拟通过→校内解锁；拒绝原因展示与重提
- [x] 校内访问控制：直链打开校内内容时被拦截并引导认证
- [x] 运行 `pnpm -C pangen-ai-compass lint`

## L. 规格归档与同步（Specs/PRD）
- [x] 在 `openspec/Specs/` 新增：`Spec-用户中心-收藏设置-额度与认证.md`（对齐实现，写全细节）
- [x] 在 `openspec/Specs/` 新增：`Spec-校内专区-访问控制与演示数据.md`（对齐实现，写全细节）
- [ ] 按 `04-规格增量.md` 将增量同步到 PRD（用户后台/校内专区/验收标准）
- [ ] 更新 `openspec/Specs/主规格.md` 索引（必要时）
